
  <%- include('partials/head' , { title: 'Ingresar' , styles: [ 'registro-login' , 'header' , 'footer' ]}) %>
  
    <body>
      <%- include('partials/header') %>
     
        <div class="loginFrm">
          <form action="/users/login"  method="post" class="form">
            <h1 class="title">Ingresa</h1>
  
  
            <div class="inputContainer">
              <input type="email" class="input" name="email" placeholder="ejemplo@gmil.com">
              <label for="" name="email" class="label">Email</label>
              <!-- <% if (locals.errors && errors.email) { %>
                <span class="errors">
                <% errors.email.msg %>
                </span>
              <% } %> -->
            </div>
  
            <div class="inputContainer">
              <input type="password" name="password" class="input" placeholder="a" minlength="6" required>
              <label for="" class="label">Contraseña</label>
              <!-- <% if (locals.errors && errors.password) { %>
                <div class="errors">
                <% errors.password.msg %>
              </div>
             <% } %> -->
             <% if (typeof errors != 'undefined') { %>
              <p style='color:red'> ERROR</p>
              <ul>
               <% for( let i = 0; i < errors.length; i++ ) { %>
               <li>
                 <%= errors[i].msg %>
               </li>
               <% } %>
              </ul>
             <% } %>
            </div>
  
            <input type="submit" class="submitBtn" value="Continuar">
  
            <br><br>
            <a href="/users/register" style="color:red;">¿No tienes cuenta? Registrate</a>
            <br>
            <br>
            <a href="#" style="color:red;">¿Olvidaste tu contraseña?</a>
          </form>
        </div>
        <%- include('partials/footer') %>
    </body>
  
    </html>